{
  "name": "balanced-gym-app",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "engines": {
    "node": "12.14.1",
    "npm": "6.13.4",
    "yarn": "1.19.1"
  },
  "author": "Pablo Pesce <papesce@gmail.com>",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run dev --parallel",
    "dev:server": "lerna run dev:server",
    "start": "lerna run start",
    "build:model": "lerna run build:model",
    "build": "lerna run build:model && lerna run build --parallel",
    "storybook": "lerna run storybook"
  }
}