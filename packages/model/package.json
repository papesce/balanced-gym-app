{
  "name": "balanced-gym-model",
  "version": "0.4.0",
  "description": "Balanced Gym App Model",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build:model": "rm -rf ./dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": " (Pablo Pesce)",
  "license": "MIT",
  "devDependencies": {
    "@types/mongoose": "^5.7.0",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "mongoose": "^5.8.11"
  }
}
